<%= render "devise/shared/header" %>
<%= form_with model: @user,url: user_session_path,local:true do |f| %>
  <h1 class="login-h1">ログイン</h1>
  <div class="sign-container">
    <div class="sign-left">
      <p class="custom-load lead">
        店舗用メールアドレス・PassWordの入力は必須です。
      </p>
      <p class="custom-load lead">
        異動して間もない担当者は異動前店舗用でログインしないよう
      </p>
      <p class="custom-load lead">
        注意してください。。
      </p>
      <p class="custom-load lead">
        ※ログインできないときは SV へ連絡をお願いします。
      </p>
    </div>
    <div class="sign-right">
      <div class="row mb-3">
        <label for="inputEmail3" class="custom-label label-change">メールアドレス</label>
        <%= f.email_field :email, class: "form-change form-control", id: "inputEmail3",placeholder: "店舗用アドレスを入力", autofocus:true %>
      </div>
      <div class="row mb-3">
        <label for="password" class="custom-label label-change">パスワード(6文字以上)</label>
        <%= f.password_field :password, class: "form-change form-control", id: "password", placeholder: "6文字以上の半角英数字" %>
      </div>
      <div class="sign-btn">
        <%= f.submit "ログイン", class: "sign-in" %>
      </div>
    </div>
  </div>
<% end %>

